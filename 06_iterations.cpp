#include <iostream>

using namespace std;

int main(){
    int index = 0;
    while(index < 34){
        cout<<"Index is :"<<index<<endl;
        index++;
    //The above is a while loop in cpp
    //Follows the same syntax of while loop for any other language

    int idx;
    for(idx = 0; idx <20; idx++)
    cout<<"value of idx is : "<<idx<<endl;
    }

    return 0;
}